DATABASE_URL="postgresql://ebarangay:ebarangay@localhost:5432/ebarangay"
AUTH_SECRET="replace-with-a-long-random-string"
AUTH_URL="http://localhost:3000"
UPLOAD_DIR="./uploads"
# Demo/Vercel only: private Supabase Storage. Leave unset locally to keep ./uploads.
# SUPABASE_URL="https://your-project.supabase.co"
# SUPABASE_SERVICE_ROLE_KEY="your-service-role-key"
# SUPABASE_STORAGE_BUCKET="ebarangay"
# Optional: live announcement email (Resend) and SMS (Semaphore). Without these,
# notices still appear in the resident portal and are recorded on the announcement.
# Resend test mode: from onboarding@resend.dev can only deliver to YOUR Resend account email.
# Verify a domain in Resend to send to every resident Gmail.
# RESEND_API_KEY="re_xxxxxxxx"
# ANNOUNCE_FROM_EMAIL="Barangay Hall <onboarding@resend.dev>"
# SEMAPHORE_API_KEY=""
